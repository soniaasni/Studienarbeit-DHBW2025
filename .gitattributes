# Default: Windows-kompatible Zeilenenden für Text-Dateien
* text=auto

# Shell-Skripte immer mit LF speichern (für Linux/Docker)
*.sh text eol=lf
*.bash text eol=lf

# Dockerfile immer LF
Dockerfile text eol=lf
docker-compose.yml text eol=lf
